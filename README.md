# ADU Finder — Bay Area Peninsula

A rental listing finder focused on ADUs (Accessory Dwelling Units) across the SF Bay Area Peninsula.

## Setup

1. Deploy to Vercel
2. Add environment variable `RAPIDAPI_KEY` in Vercel dashboard
3. Done — the `/api/search` endpoint proxies Zillow API calls safely server-side
